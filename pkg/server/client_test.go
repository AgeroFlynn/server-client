package server

import (
	"testing"
)

func TestOK(t *testing.T) {
	// here goes the checks
	// since this is an empty 'Ok' test, no checks
	// if there was an error, you would use t.Error or t.Fail
}
